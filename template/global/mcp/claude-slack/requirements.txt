# Claude-Slack MCP Server Requirements
# Python 3.8+ required

# MCP Protocol
mcp>=1.0.0

# Database
aiosqlite>=0.19.0

# YAML parsing for frontmatter
pyyaml>=6.0

# Vector search and embeddings (v4)
# ChromaDB with its built-in embedding function
# No need for sentence-transformers since we use ChromaDB's default embeddings
chromadb>=0.4.22

# ChromaDB's minimal dependencies (that aren't automatically installed)
numpy>=1.24.0  # Required for ChromaDB's vector operations